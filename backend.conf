# Database server address
host=127.0.0.1

# Database server port
port=3306

# Database name
database=test

# Database username
username=test

# Database user
password=test

# Enable/disable file based logging
log-file=false

# Log file directory
log-dir=/var/log/pdns

# Redis server host
cache-host=127.0.0.1

# Redis server port
cache-port=6379

# Default landing page
landing-page=127.0.0.1

# Debugging
debug=true

# Filtering status
filter=false

# Whether to use recursor or not, when used, backend will not resolve 
# any domain name to get records but send it directly to server (recursor)
use-recursor=false

# If set then when resolve error (eg. NXDOMAIN, SERVFAIL, etc.), backend
# will not send FAIL response to server, but send record for redirect
# address informations which defined by redirect-error-address option
redirect-error=false

# Address of redirect error
redirect-error-address=127.0.0.1

# File of list of recursor to use, file format is similar to standard resolv.conf
# only used either use-recursor=false or redirect-error=true
recursor-file=/etc/resolv.conf
